A MD5 crack programma intrelly written in C. It is pretty fast but it is not omptimized for GPUs.
It uses a non-casual brute force and it takes three command line arguments: <hash> <starting string> <first letter><last letter>
